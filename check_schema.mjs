import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config(); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_SERVICE_ROLE_KEY); async function run() { const {data, error} = await supabase.from('leave_requests').select('*').limit(1); console.log(data ? 'Table exists in public schema' : 'Table missing from public schema', error); } run();
